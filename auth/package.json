{
  "name": "auth-service",
  "version": "1.0.0",
  "description": "Better Auth authentication service for Physical AI Book chatbot",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc",
    "migrate": "npx @better-auth/cli migrate",
    "generate": "npx @better-auth/cli generate"
  },
  "dependencies": {
    "better-auth": "^1.2.0",
    "pg": "^8.13.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
